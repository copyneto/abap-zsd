@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZI_SD_CAFT_PARAMETROS {
    grant 
        select
            on
                ZI_SD_CAFT_PARAMETROS
                    where
                        ( ) = aspect pfcg_auth(ZSDMTABLE, ACTVT = '03', TABLE = 'ZTSD_CAFT_PARAM');
                        
}