@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZI_SD_CAFT_TPCOND {
    grant 
        select
            on
                ZI_SD_CAFT_TPCOND
                    where
                        ( ) = aspect pfcg_auth(ZSDMTABLE,ACTVT = '03', TABLE = 'ZTSD_CAFT_TPCOND' );
                        
}