@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZI_SD_CALCULO_DESONERADO {
    grant 
        select
            on
                ZI_SD_CALCULO_DESONERADO
                    where
                       ( ) = aspect pfcg_auth( ZSDMTABLE, ACTVT = '03', TABLE = 'ZTSD_CBENEF' );
                        
}