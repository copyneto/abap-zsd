@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZI_SD_IONZ_PARCEI {
    grant 
        select
            on
                ZI_SD_IONZ_PARCEI
                    where
                        ( ) = aspect pfcg_auth( ZSDMTABLE, ACTVT = '03', TABLE = 'ZTSD_IONZ_PARCEI' );
                        
}