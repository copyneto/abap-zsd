@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZI_SD_CALC_DIFAL {
    grant 
        select
            on
                ZI_SD_CALC_DIFAL
                    where
                        ( ) = aspect pfcg_auth( ZSDMTABLE, ACTVT = '03', TABLE = 'ZTSD_CALC_DIFAL' );
                        
}