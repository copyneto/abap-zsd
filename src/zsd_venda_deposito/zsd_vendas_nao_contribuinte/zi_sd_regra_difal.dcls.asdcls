@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZI_SD_REGRA_DIFAL {
    grant 
        select
            on
                ZI_SD_REGRA_DIFAL
                    where
                        (  ) = aspect pfcg_auth( ZSDMTABLE, ACTVT = '03', TABLE = 'ZTSD_REGRA_DIFAL' );
                        
}